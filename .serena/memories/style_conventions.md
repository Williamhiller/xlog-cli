# Style and conventions
- Use concise ESM modules and functional React components.
- Viewer styling is plain CSS with shared classnames, not utility classes.
- Prefer editing shared CSS surfaces instead of per-page one-offs when changing UI language.
- Keep xlog naming current; avoid legacy aliases unless explicitly requested.
- Preserve local-first behavior, server defaults, and storage/capture semantics unless the task asks otherwise.